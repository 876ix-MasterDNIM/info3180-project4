var registerApp = angular.module( 'registerApp', ['ngRoute', 'ngMessages'] );

registerApp.config( ['$routeProvider', function ( $routeProvider ) {
    $routeProvider.
        when( '/register', {

        } );
}] );

